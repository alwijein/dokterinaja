import 'package:flutter/material.dart';

SelectedItem(BuildContext context, item) {
  switch (item) {
    case 0:
      // run for telepon
      break;
    case 1:
      // run for massage
      break;
  }
}
